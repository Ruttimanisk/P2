extends layout_admin

block content
    h1 Vagtplan

    link(rel='stylesheet', href='https://cdn.jsdelivr.net/npm/@fullcalendar/resource-timeline/index.global.min.css')
    script(src='https://cdn.jsdelivr.net/npm/@fullcalendar/core@6.1.8/index.global.min.js')
    script(src='https://cdn.jsdelivr.net/npm/@fullcalendar/resource-timeline@6.1.8/index.global.min.js')

    div#calendar(style="max-width: 900px; margin: 40px auto;")

    script.
        console.log("💬 Events JSON string:", !{events});
        console.log("📋 Resources JSON string:", !{resources});
        try {
            const events = !{events};
            const resources = !{resources};

            document.addEventListener('DOMContentLoaded', function () {
                var calendarEl = document.getElementById('calendar');
                var calendar = new FullCalendar.Calendar(calendarEl, {
                    initialView: 'resourceTimelineWeek',
                    allDaySlot: false,
                    slotMinTime: "07:00:00",
                    slotMaxTime: "20:00:00",
                    events: events,
                    resources: resources
                });

                calendar.render();
            });
        } catch (err) {
            console.error("❌ Fejl i JSON:", err);
        }
