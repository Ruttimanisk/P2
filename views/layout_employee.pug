doctype html
html
    head
        meta(charset='utf-8')
        title WFM App
        meta(name='viewport' content='width=device-width, initial-scale=1')
        link(rel='stylesheet' href='/CSS/HOME%20ADMIN.css')
        link(rel='stylesheet' href='/CSS/Shared.css')
    body
        script.
            function confirmLogout() {
                return confirm("Are you sure you want to log out?");
            }
        .topnav
            a(href='/employee/home') Home
            a(href='/employee/profile') Profile
            a(href='/employee/schedule') Schedule
            a(href="/employee/login", onclick= "confirmLogout()") Logout
        img(src='/Images/WFM.webp' alt='WFM Logo')
    block content