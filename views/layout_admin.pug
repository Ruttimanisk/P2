doctype html
html
    head
        meta(charset='utf-8')
        title WFM App
        meta(name='viewport' content='width=device-width, initial-scale=1')
        link(rel='stylesheet' href='/stylesheets/HOME%20ADMIN.css')
        //link(rel='stylesheet' href='/stylesheets/Shared.css')

        link(rel='stylesheet', href='https://cdn.jsdelivr.net/npm/fullcalendar@6.1.8/main.min.css')

        block head

    body
        script.
            function confirmLogout() {
                return confirm("Are you sure you want to log out?");
            }

        .topnav
            a(href='/admin/home') Home
            a(href='/admin/profile') Profile
            a(href='/admin/schedule') Schedule
            a(href='/admin/employees_list') List of Employees
            a(href='/admin/edit_employee_schedule') Edit Employee Schedule
            a(href='/admin/user_creation') User Creation
            a(href='/login', onclick="return confirmLogout()") Logout

        .sidebar
            img(src='/images/WFM.webp' alt='WFM Logo')

        block content

        script(src='https://cdn.jsdelivr.net/npm/fullcalendar@6.1.8/main.min.js')

        block scripts
